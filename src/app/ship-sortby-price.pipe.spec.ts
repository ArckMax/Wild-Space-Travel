import { ShipSortbyPricePipe } from './ship-sortby-price.pipe';

describe('ShipSortbyPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ShipSortbyPricePipe();
    expect(pipe).toBeTruthy();
  });
});
